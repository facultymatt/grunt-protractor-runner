describe('Many instances test', function() {
  
  it('should run in safari', function() {
    //expect(browser.getCapability('browserName')).toEqual('safari');
  });
  it('should run in firefox', function() {
    //expect(browser.getCapability('browserName')).toEqual('firefox');
  });
  it('should run in chrome', function() {
    //expect(browser.getCapability('browserName')).toEqual('chrome');
  });
});
